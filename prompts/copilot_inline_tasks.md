# Copilot Inline Task Prompt — Industrial: Models that learn how processes unfold

Use Copilot inside VS Code for small contained tasks only.

Good Copilot tasks:
- implement missing function from docstring;
- add type hints;
- write unit tests for one module;
- make Streamlit UI cleaner;
- add validation and error messages;
- add small helper for metrics or plotting.

Copy-paste task format:

Implement this function only. Keep it simple, typed, and tested. Do not change other files.
Context: Train a sequence model that learns industrial process trajectories and predicts the next states, bottlenecks, or anomalies.
Inputs:
Outputs:
Edge cases:
Tests to add:
