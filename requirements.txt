numpy<2
pandas
scikit-learn==1.6.1
matplotlib
streamlit
plotly
pydantic
pyyaml
tqdm
pytest
ruff
# install torch separately for target GPU/CUDA environment if needed
# Required for compact Transformer local CPU smoke/small runs.
# On Leonardo/GPU, install the CUDA-enabled PyTorch build according to the HPC docs.
torch
